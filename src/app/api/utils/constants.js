export const validations = {
    mustBeRequired: "mustBeRequired",
    mustNotBeEmpty: "mustNotBeEmpty",
    mustBeValidEmail: "mustBeValidEmail",
    mustBeValidRequestBody: "mustBeValidRequestBody",
    mustNotBeEmptyBody: "mustNotBeEmptyBody",
    mustBeString: "mustBeString",
    mustBeValidPassword: "mustBeValidPassword",
    MustBeEqualValue: "MustBeEqualValue"
}

export const response = {
    error: "ERROR",
    success: "SUCCESS"
}

export const errorType = {
    ValidationError: "ValidationError",
    InternalServerError: "InternalServerError",
    SyntaxError: "SyntaxError",
    InformativeError: "InformativeError"
}

export const defaultMessage = {
    InternalServerError: "unable to process request",
    SyntaxError: "Request Body must be of valid format"
}

export const storedProcedure = {
    register: "register"
}